name: Multiple line commands & push event
on: push 
jobs: 
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Print multiple line code
              run: |
                echo Print line one
                echo '${{toJson(github)}}' 