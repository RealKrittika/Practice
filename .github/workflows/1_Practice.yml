name: 1_Creating workflow & using multiple triggers
on: workflow_dispatch
jobs: 
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: Print Hello World
              run: echo "Hello, World!" 
                